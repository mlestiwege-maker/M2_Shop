// backend/controllers/complaintController.js

export const getComplaints = (req, res) => {
  res.json({ message: "Get all complaints (dummy)" });
};

export const createComplaint = (req, res) => {
  res.json({ message: "Create a complaint (dummy)" });
};
