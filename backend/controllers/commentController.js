// backend/controllers/commentController.js

export const getComments = (req, res) => {
  res.json({ message: "Get all comments (dummy)" });
};

export const createComment = (req, res) => {
  res.json({ message: "Create a comment (dummy)" });
};
